#!/bin/bash
# P2P対戦テスト用: web版で2タブ

# matchbox_server が無ければ起動
if ! pgrep -f matchbox_server > /dev/null; then
  echo "matchbox_server を起動..."
  matchbox_server &
  sleep 1
fi

echo "Web dev server 起動中..."
echo "ブラウザで2つのタブを開いてテストしてください"
echo ""
echo "  Ctrl+C で終了"
echo ""

bevy run web
